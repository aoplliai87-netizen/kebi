export default function AdminPage() {
  return (
    <div className="mx-auto max-w-content px-4 py-10">
      <h1 className="text-xl font-semibold">관리자 (준비 중)</h1>
      <p className="mt-2 text-sm text-muted">
        다국어 라우트 외부 · 이후 가격·배차 UI를 여기에 연결합니다.
      </p>
    </div>
  );
}
